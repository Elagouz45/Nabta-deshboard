import{computed,Injectable,signal}from'@angular/core';import{Article,ArticleFormValue,ArticleStatus}from'../models/article.models';import{DEMO_ARTICLES}from'./articles.demo';@Injectable({providedIn:'root'})export class ArticlesStore{private readonly rowsState=signal<readonly Article[]>(DEMO_ARTICLES);readonly rows=this.rowsState.asReadonly();readonly search=signal('');readonly status=signal<ArticleStatus|''>('');readonly feedback=signal<string|null>(null);readonly filtered=computed(()=>{const q=this.search().trim().toLocaleLowerCase('ar');return this.rowsState().filter(item=>(!q||[item.title,item.author,item.category].some(value=>value.toLocaleLowerCase('ar').includes(q)))&&(!this.status()||item.status===this.status()))});readonly publishedCount=computed(()=>this.rowsState().filter(item=>item.status==='published').length);readonly reviewCount=computed(()=>this.rowsState().filter(item=>item.status==='in_review').length);readonly draftCount=computed(()=>this.rowsState().filter(item=>item.status==='draft').length);find(id:string|null):Article|null{return this.rowsState().find(item=>item.id===id)??null}save(value:ArticleFormValue,id:string|null,status:ArticleStatus):string{const article:Article={id:id??crypto.randomUUID(),title:value.title,slug:value.slug,summary:value.summary,body:value.body,category:value.category,author:'مها محمد',status,updatedAt:'الآن',publishAt:value.publishAt||null,views:this.find(id)?.views??0,featured:value.featured};this.rowsState.update(rows=>id?rows.map(item=>item.id===id?article:item):[article,...rows]);this.feedback.set(status==='in_review'?'تم إرسال المقال للمراجعة في الوضع التجريبي.':'تم حفظ مسودة المقال.');return article.id}}
