import { ChangeDetectionStrategy, Component, input } from '@angular/core';
import { ReturnStatus, RETURN_STATUS_LABELS } from '../../models/returns.models';
@Component({ selector: 'app-return-status-badge', changeDetection: ChangeDetectionStrategy.OnPush, template: `<span class="badge" [class]="'badge badge--' + status()">{{ labels[status()] }}</span>`, styles: [`.badge{display:inline-flex;align-items:center;justify-content:center;width:7.2rem;min-height:1.75rem;border-radius:999px;font-size:.75rem;font-weight:600;white-space:nowrap}.badge--new{background:#e1f1fa;color:#267098}.badge--awaiting_receipt,.badge--inspecting{background:#fff0d4;color:#a76508}.badge--approved{background:#e3f2e8;color:#237048}.badge--rejected{background:#fbe0e0;color:#b53131}`] }) export class ReturnStatusBadge { readonly status = input.required<ReturnStatus>(); readonly labels = RETURN_STATUS_LABELS; }
