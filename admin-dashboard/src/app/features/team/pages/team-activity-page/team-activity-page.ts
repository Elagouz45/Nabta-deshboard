import{ChangeDetectionStrategy,Component,inject}from'@angular/core';import{LucideActivity,LucideChevronLeft,LucideClock3,LucideSearch,LucideX}from'@lucide/angular';import{TeamStore}from'../../data-access/team.store';
@Component({selector:'app-team-activity-page',imports:[LucideActivity,LucideChevronLeft,LucideClock3,LucideSearch,LucideX],providers:[TeamStore],changeDetection:ChangeDetectionStrategy.OnPush,template:`<main class="team-page" dir="rtl"><header><div><small>إدارة الفريق</small><h1>سجل نشاط الفريق</h1><p>راجع العمليات التي نفّذها أعضاء الفريق داخل لوحة الإدارة.</p></div></header><section class="panel"><div class="toolbar"><label><svg lucideSearch></svg><input placeholder="ابحث بالموظف أو العملية أو العنصر" (input)="store.search.set($any($event.target).value)"></label><select (change)="store.module.set($any($event.target).value)"><option value="">كل الأقسام</option><option>الطلبات</option><option>المنتجات</option><option>العملاء</option><option>المحتوى</option><option>التقارير</option></select></div><div class="table"><table><thead><tr><th>الموظف</th><th>العملية</th><th>القسم</th><th>العنصر المتأثر</th><th>الفرع</th><th>الوقت</th><th></th></tr></thead><tbody>@for(item of store.filteredActivity();track item.id){<tr><td><div class="person"><i>{{store.initials(item.employee)}}</i><b>{{item.employee}}</b></div></td><td><em class="action">{{item.action}}</em></td><td>{{item.module}}</td><td>{{item.entity}}</td><td>{{item.branch}}</td><td><span class="time"><svg lucideClock3></svg>{{item.occurredAt}}</span></td><td><button class="more" type="button" [attr.aria-label]="'عرض تفاصيل '+item.entity" (click)="store.selectedActivity.set(item)"><svg lucideChevronLeft></svg></button></td></tr>}</tbody></table></div></section>@if(store.selectedActivity();as item){<div class="activity-backdrop" (click)="store.selectedActivity.set(null)"><aside role="dialog" aria-modal="true" (click)="$event.stopPropagation()"><header><div><h2>تفاصيل النشاط</h2><p>{{item.occurredAt}}</p></div><button type="button" aria-label="إغلاق" (click)="store.selectedActivity.set(null)"><svg lucideX></svg></button></header><span class="activity-icon"><svg lucideActivity></svg></span><h3>{{item.action}} — {{item.entity}}</h3><p>{{item.description}}</p><dl><div><dt>الموظف</dt><dd>{{item.employee}}</dd></div><div><dt>القسم</dt><dd>{{item.module}}</dd></div><div><dt>الفرع</dt><dd>{{item.branch}}</dd></div></dl></aside></div>}</main>`,styleUrl:'../team-page.scss'})export class TeamActivityPage{readonly store=inject(TeamStore)}
