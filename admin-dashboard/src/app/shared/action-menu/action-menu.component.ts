import{ChangeDetectionStrategy,Component,ElementRef,HostListener,input,output,signal,viewChild}from'@angular/core';
export interface ActionMenuItem{readonly id:string;readonly label:string;readonly disabled?:boolean;readonly danger?:boolean;}
@Component({selector:'app-action-menu',changeDetection:ChangeDetectionStrategy.OnPush,host:{class:'action-menu-host'},template:`<button #trigger class="trigger" type="button" [attr.aria-label]="label()" aria-haspopup="menu" [attr.aria-expanded]="open()" (click)="toggle()"><span aria-hidden="true">•••</span></button>@if(open()){<div class="menu" role="menu">@for(item of items();track item.id){<button type="button" role="menuitem" [disabled]="item.disabled" [class.danger]="item.danger" (click)="choose(item)">{{item.label}}</button>}</div>}`,
styles:[`:host{position:relative;display:inline-flex}.trigger{display:grid;place-items:center;width:2rem;height:2rem;border:1px solid var(--border);border-radius:.55rem;background:#fff;color:var(--forest);font-weight:800;cursor:pointer}.trigger:hover,.trigger[aria-expanded=true]{background:var(--mint)}.menu{position:absolute;z-index:35;inset-inline-end:0;top:calc(100% + .3rem);display:grid;min-width:10.5rem;padding:.3rem;border:1px solid var(--border);border-radius:.65rem;background:#fff;box-shadow:0 10px 28px rgba(38,54,47,.12)}.menu button{min-height:2.35rem;padding:.45rem .7rem;border:0;border-radius:.45rem;background:transparent;color:var(--text);text-align:start;font-size:.74rem;cursor:pointer}.menu button:hover{background:var(--pale)}.menu button.danger{color:var(--danger)}.menu button:disabled{opacity:.45;cursor:not-allowed}`]})
export class ActionMenuComponent{readonly items=input.required<readonly ActionMenuItem[]>();readonly label=input('الإجراءات');readonly selected=output<string>();readonly open=signal(false);private readonly trigger=viewChild.required<ElementRef<HTMLButtonElement>>('trigger');toggle():void{this.open.update(value=>!value);}choose(item:ActionMenuItem):void{if(item.disabled)return;this.open.set(false);this.selected.emit(item.id);}@HostListener('document:click',['$event'])closeOutside(event:Event):void{const target=event.target;if(target instanceof Node&&!this.trigger().nativeElement.parentElement?.contains(target))this.open.set(false);}@HostListener('document:keydown.escape')close():void{if(this.open()){this.open.set(false);this.trigger().nativeElement.focus();}}}
