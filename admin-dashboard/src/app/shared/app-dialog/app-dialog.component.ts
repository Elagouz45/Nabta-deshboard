import{ChangeDetectionStrategy,Component,ElementRef,HostListener,afterNextRender,input,output,viewChild}from'@angular/core';
@Component({selector:'app-dialog',changeDetection:ChangeDetectionStrategy.OnPush,host:{class:'app-dialog-host'},template:`<div class="backdrop" (click)="closed.emit()"><section #panel role="dialog" aria-modal="true" [attr.aria-labelledby]="dialogId" tabindex="-1" (click)="$event.stopPropagation()"><header><div><h2 [id]="dialogId">{{title()}}</h2>@if(subtitle()){<p>{{subtitle()}}</p>}</div><button type="button" aria-label="إغلاق" (click)="closed.emit()">×</button></header><div class="body"><ng-content/></div><footer><ng-content select="[dialog-actions]"/></footer></section></div>`,styles:[`:host{position:fixed;inset:0;z-index:100}.backdrop{display:grid;place-items:center;width:100%;height:100%;padding:1rem;background:rgba(27,45,36,.44)}section{width:min(32rem,100%);max-height:calc(100dvh - 2rem);overflow:auto;border:1px solid var(--border);border-radius:var(--radius);background:#fff;box-shadow:0 22px 70px rgba(22,40,31,.2)}header{display:flex;align-items:flex-start;justify-content:space-between;gap:1rem;padding:1.1rem 1.2rem;border-bottom:1px solid var(--border)}h2{margin:0;color:var(--forest);font:700 1rem var(--font-display)}p{margin:.2rem 0 0;color:var(--muted);font-size:.72rem}header button{border:0;background:transparent;color:var(--muted);font-size:1.45rem;cursor:pointer}.body{padding:1.2rem}footer{display:flex;justify-content:flex-end;gap:.55rem;padding:1rem 1.2rem;border-top:1px solid var(--border);background:#fafcfa}footer:empty{display:none}`]})
export class AppDialogComponent{readonly title=input.required<string>();readonly subtitle=input('');readonly closed=output<void>();readonly dialogId=`dialog-${Math.random().toString(36).slice(2)}`;private readonly panel=viewChild.required<ElementRef<HTMLElement>>('panel');constructor(){afterNextRender(()=>this.panel().nativeElement.focus());}@HostListener('document:keydown.escape')escape():void{this.closed.emit();}}
